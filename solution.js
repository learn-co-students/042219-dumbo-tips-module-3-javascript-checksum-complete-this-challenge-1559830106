var data = [
  [409, 194, 207, 470, 178],
  [454, 235, 333, 511, 103],
  [474, 293, 525, 372, 408],
  [428, 4321, 2786, 6683, 3921],
  [265, 262, 6206, 2207, 5712]
]


function checkSum(data) {
	let sum = [];
}

data.forEach(function(row) {
  let max = Math.max.apply(null, row)
  let min = Math.min.apply(null, row)
});


let max = Math.max.apply(null, data[i])
let min = Math.min.apply(null, data[i])
let ans = max - min
sum.push(ans);
sum.reduce
return sum



data.forEach(function(array) {
	array.forEach(function(row){
		let max = Math.max.apply(null, row)
  		let min = Math.min.apply(null, row)
  		let ans = max - min
		sum.push(ans);
	})
	sum.reduce
	return sum
});



data.forEach(function(array) {
	for (i = 0; i < array.length; i++) {
		let max = Math.max.apply(null, data)
		let min = Math.min.apply(null, data)
		let ans = max - min
	}
	console.log(ans)
});




var arr = [1,2,3];
var max = arr.reduce(function(a, b) {
    return Math.max(a, b);
});





data.forEach(function(arrays){
	arrays.forEach(function(array){
		let max = array.reduce(function(a, b){
			return Math.max(a, b)
		});
	});
});







